# VoiceRay MFA worker stub — local dev only
services:
  mfa:
    build: .
    ports:
      - "8765:8765"
    environment:
      VOICERAY_MFA_MODE: stub
    healthcheck:
      test: ["CMD", "./healthcheck.sh"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
