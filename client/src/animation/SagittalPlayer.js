/**
 * Phase 0 stub — sagittal SVG animation arrives in Phase 1.
 */
export class SagittalPlayer {
  /**
   * @param {SVGElement | null} root
   */
  constructor(root) {
    this.root = root
  }

  /** @param {unknown[]} _keyframes */
  playKeyframes(_keyframes) {
    // no-op until vocal-tract.svg layers are wired
  }
}
