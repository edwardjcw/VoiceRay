{
  "name": "client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test && npm run test:e2e && npm run test:e2e:integration",
    "test:unit": "node --test",
    "test:e2e": "playwright test",
    "test:e2e:integration": "npm run build && playwright test -c playwright.integration.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "vite": "^8.0.12"
  }
}
